// repeating a set of actions while a condition is true is the job of loops
// the execution of a block of code inside a loop is called an iteration

numberOfCustomers = 5;

// while (numberOfCustomers > 0) {
//   console.log("Hello, How May I Assist You?");

//   numberOfCustomers = numberOfCustomers - 1;
// }

// OR

// do {
//   console.log("Hello, How May I Assist You?");
//   numberOfCustomers = numberOfCustomers - 1;
// } while (numberOfCustomers > 0);

// the difference between a while loop and a do... while loop
// unlike a while loop a do while loop will run the first time even if while loop returns false

// the for loop

for (var i = 0; i <= 9; i = i + 1) {
  console.log(i);
}

// the for loop has three clauses

// the initialization clause (var i = 0)
// the condition clause (i <= 9)
// the update clause (i = i + 1)
