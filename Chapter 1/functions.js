// a function is a named block of code that can be "called"

// function printAmount() {
//   console.log(amount.toFixed(2));
// }

// var amount = 99.99;

// printAmount();

// amount = amount * 2;

// printAmount();

// functions can also take parameters aka arguments and can also optionally return a value

// function printAmount(amt) {
//   console.log(amt.toFixed(2));
// }
// function formatAmount() {
//   return "$" + amount.toFixed(2);
// }
// var amount = 99.99;
// printAmount(amount * 2);
// amount = formatAmount();
// console.log(amount);

// const TAX_RATE = 0.08;

// function calcFinalPurchaseCost(amt) {
//   amt = amt + amt * TAX_RATE;
//   return amt;
// }

// amount = 100;
// amount = calcFinalPurchaseCost(amount);

// console.log(amount);
